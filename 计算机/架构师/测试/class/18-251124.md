1.randint(a,b) 随机输出int x，a<=x<=b
2.三元表达式赋值。 a if 条件 else b 
    等价于 if 条件：a else ： b 
3.反转字符
```python
1.str =str[::-1]
2.for i in range(len(str),-1,-1) 
	print(i)
3.str = list(str)
str.reverse()
str_new = ''.join(str)
```
4.列表推导式
```python
1.list [表达式 for 变量 in 可迭代数据 if 条件]
```
5.