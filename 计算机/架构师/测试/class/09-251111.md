1.dql
	select `*`from tb where 条件 group by 字段 having 字段 order by 字段 limit 1
2.去重
	select distinct 字段
3.！= 等价于 <>
4.模糊查询 
	'张%'  姓张的所有同学
	'张__'  姓张的两位名字的同学
	'%张'  名字包含张的同学
5.order by 
	排序时依此从左往右为主来排序
	select num1,num2 from tb order by num1,num2 desc
	先按照num1从小到大正序排列，有重复时再按照num2从大到小倒叙排列
6.判断是否为空时，空为null
7.where在分组前执行，having在分组后执行且可跟聚合函数，因此having与order by几乎绑定