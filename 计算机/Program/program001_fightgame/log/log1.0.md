fightgame，尚未命名，下文简称为FG
# 1.0
基于石头剪刀布的运作模式，演变为兵器和招式对决，用户通过输入来进行"猜拳"。

FG通过react模块来提取输入的关键字来输出对应招式。在目前版本只存在拳、剑、刀。其克制关系为拳可剑、剑可刀、刀可拳。

# 1.1
增加除招式外额外选项，现已有'帮助'和'逃跑'两个额外选项，出现关键词就会触发相应对话对玩家进行提示。并内置更多进阶招式，通过隐性的内置积分触发。

# 1.2
用户输入提取关键字不是很美观，用户大量输入却只能选择其中的一个关键词，因此决定放弃自主输入，而是通过声明menu提供选择菜单。并更改react函数的使用逻辑。同时内置多个函数

# 1.3
美化菜单页面，使用内部类Menu来管理菜单页面，现在菜单页面分为主菜单和攻击菜单，并增加更多内置函数

# 1.4
增加新内部类Attribute并内置多个函数，增加对血量和伤害的描述，游戏可分出胜出而不必每次都按逃跑

# 1.5
## 1.5.1
实装defense防御，并增加伤害和受击反馈，修复多个bug
## 1.5.2
将积分作为Attribute中的一个新属性能量而非Game大类
## 1.5.3
将energy封装，外部调用只能通过Attribute中的energy_get，energy_set，energy_do三个函数

# 1.6
将能量的使用规范化，通过energy_do来控制全局能量的增加或减少，并增加额外的能量回复策略
## 1.6.1
对招式进行更新，将attact和defense全部放入skill大字典中，并增加了字典的深度检索并保持对旧接口的兼容
# 1.7
将旧字典全部合并在新字典skill中，更新全部接口，删除了所有旧接口，并添加大量新的接口
## 1.7.1
解耦，将主文件py中的数据移植到项目其他位置，增加新的工程文件，增加重要量constants.py和配置文件config.json，和skill.json

# 1.8
解耦更加彻底，创立一个新的combat来专职负责战斗
┌─────────────────────────────────────────┐
│  Game.main()                            │
│  (主循环，唯一入口)                      │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Game.fight(player_skill)               │
│  - 调用战斗系统                          │
│  - 处理额外效果（如重伤提示）            │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Combat.execute_turn()                  │
│  - 流程控制：准备→玩家行动→PC行动→结算  │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Combat._build_combat_result()          │
│  - 实际的克制判断                        │
│  - 伤害计算与结算                        │
│  - 能量更新                              │
└─────────────────────────────────────────┘

## 1.8.1
对元数据的获取进行更改，将对skill字典的遍历和对缓存字典的创建区分，并更新的get_skill查询方法
## 1.8.2
规范constants的调用，修改energy_的能量相关调用
# 1.9
将fight解耦，移植与/system/conbat.py中，并增加新的概念'阶段'，现在分为准备阶段，玩家阶段，pc阶段，结算阶段
## 1.9.1
将fight和judge完全解耦，将属性相关函数damage_，energy_等移植于attribute中
